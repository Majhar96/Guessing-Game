var names = ['raju', 'anis', 'mujo', 'jafar'];
console.log(names);

// Shift opposite of pop;
// names.shift();
// console.log(names);
// unshift opposite of push;
// names.unshift('shagor');
// console.log(names);

// splice element add and remove;

// adding system
// names.splice(2, 0, 'hena', 'keya');
// console.log(names);

// removing system;
// names.splice(2, 3);
// console.log(names);

// Slice will slice elements and make a new array.This will not change the main array so main array will be same.

// sort/reverse method will arrange it alphabetically.sort will not follow number it will work only with alphabet.so we use a function which take two parameters and return with condition of small and big number.
// names.sort();
// names.reverse();
// console.log(names);

// var numbers = [32, 3, 53, 2, 53, 6];
// numbers.sort(function (a, b) {
//     return a - b;
// });
// console.log(numbers);



